# Optional: enable GA4 page views and events without changing the UI.
VITE_GA_MEASUREMENT_ID=

# Optional future integrations. Keep secrets in Netlify environment variables,
# never in source code.
VITE_API_BASE_URL=
VITE_CHECKOUT_URL=
VITE_EMAIL_PROVIDER=
